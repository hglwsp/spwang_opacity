# def getSum(a,b):
#     if a==0 :
#         return b
#     elif b==0:
#         return a
#     else:
#         while (b != 0) {
#             int carry = a & b; // 将存在进位的位置置1
#             a = a ^ b; // 计算无进位的结果
#         b = carry << 1;
#         }
#
#         return a;

